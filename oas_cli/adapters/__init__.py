"""Engine adapters for the Open Agent Spec runtime.

Each adapter module is responsible for talking to a concrete provider
or runtime (e.g. OpenAI, Codex, Claude) and normalising its responses
into shapes the OA runtime can work with.
"""

