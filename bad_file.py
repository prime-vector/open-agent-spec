import os, sys  # unused imports, multiple on one line


def  bad_function(  x:int ,  y:int)->int:   # spacing / formatting violations
    foo =  1+2
    return  x+y+foo  # probably fine logically, but style is bad