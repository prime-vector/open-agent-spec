# Homebrew formula stub — Open Agent Spec (OA) CLI
#
# Homebrew does not run `pip install` with network during install; dependencies
# must be declared as `resource` blocks. Generate them with:
#
#   brew update-python-resources open-agent-spec
#
# after adding a minimal formula to homebrew-core (or your tap) with:
#   url "https://files.pythonhosted.org/packages/source/o/open-agent-spec/open_agent_spec-VERSION.tar.gz"
#   sha256 "..."
#
# Easiest path for users today:
#   brew install pipx && pipx ensurepath
#   pipx install open-agent-spec
#   oa --version
#
# See docs/packaging/HOMEBREW.md for full options.
